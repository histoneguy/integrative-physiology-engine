__pycache__/
*.py[cod]
.venv/
venv/
.env
build/
dist/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
results/
*.h5
*.nc
.DS_Store
.idea/
.vscode/
.publish-config.json

# Incoming patch files are working material, not repository content.
# They also must not register as "uncommitted changes" or sprint.py will
# refuse to apply them.
incoming/*.patch

# Generated by tools/export_gui_data.jl; its content is inlined into gui/index.html,
# which IS committed because it is the deliverable and must open without Julia.
gui/model_data.json

# NHANES public microdata, 53 MB, re-downloaded on demand by
# validation/nhanes_hpt_extract.py. Public data does not belong in a git history.
validation/data/nhanes/
